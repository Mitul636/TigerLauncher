name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "TigerLauncher-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/TigerLauncher-source TigerLauncher-${{ env.VERSION }}
          mv TigerLauncher-Linux-Qt6-Portable*/TigerLauncher-portable.tar.gz TigerLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv TigerLauncher-Linux-aarch64-Qt6-Portable*/TigerLauncher-portable.tar.gz TigerLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv TigerLauncher-*.AppImage/TigerLauncher-*-x86_64.AppImage TigerLauncher-Linux-x86_64.AppImage
          mv TigerLauncher-*.AppImage.zsync/TigerLauncher-*-x86_64.AppImage.zsync TigerLauncher-Linux-x86_64.AppImage.zsync
          mv TigerLauncher-*.AppImage/TigerLauncher-*-aarch64.AppImage TigerLauncher-Linux-aarch64.AppImage
          mv TigerLauncher-*.AppImage.zsync/TigerLauncher-*-aarch64.AppImage.zsync TigerLauncher-Linux-aarch64.AppImage.zsync
          mv TigerLauncher-macOS*/TigerLauncher.zip TigerLauncher-macOS-${{ env.VERSION }}.zip
          mv TigerLauncher-macOS*/TigerLauncher.dmg TigerLauncher-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf TigerLauncher-${{ env.VERSION }}.tar.gz TigerLauncher-${{ env.VERSION }}

          for d in TigerLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="TigerLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv TigerLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in TigerLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="TigerLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv TigerLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in TigerLauncher-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="TigerLauncher-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv TigerLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Prism Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            TigerLauncher-Linux-x86_64.AppImage
            TigerLauncher-Linux-x86_64.AppImage.zsync
            TigerLauncher-Linux-aarch64.AppImage
            TigerLauncher-Linux-aarch64.AppImage.zsync
            TigerLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            TigerLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            TigerLauncher-Windows-MinGW-w64-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            TigerLauncher-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            TigerLauncher-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            TigerLauncher-Windows-MSVC-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            TigerLauncher-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            TigerLauncher-macOS-${{ env.VERSION }}.zip
            TigerLauncher-macOS-${{ env.VERSION }}.dmg
            TigerLauncher-${{ env.VERSION }}.tar.gz
